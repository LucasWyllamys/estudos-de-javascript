function areQuadrado(lado) {
    return lado * lado
}

areQuadrado.toString()  // Retorna a função string.
/* Saída: 
function areQuadrado(lado) {
    return lado * lado
}
*/
areQuadrado.length      // Saída: 1 (retorna o número de argumentos da função.)